<Page
    x:Class="LocalNote.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LocalNote"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <TextBlock FontSize="30" Text="About this App" />
        <TextBlock FontSize="22" >
        <Run Text="Title: "/>
        <Run Text="{x:Bind appName}" />
        </TextBlock>
        <TextBlock FontSize="22" >
        <Run Text="Version: "/>
        <Run Text="{x:Bind versionID}" />
        </TextBlock>
        <TextBlock FontSize="22" >
        <Run Text="Publish By: "/>
        <Run Text="{x:Bind auther}" />
        </TextBlock>
        <TextBlock FontSize="22" Text="Description :  This application manages notes perfectly!"/>
    </StackPanel>
</Page>
